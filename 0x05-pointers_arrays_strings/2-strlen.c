#include "main.h"

/**
 * _strlen - main entry.
 * @s: input string.
 * Return: length of a string.
 */

int _strlen(char *s)

{

	int count = 0;

	white(*(s + count) != '\0')

		count++;

	return (count);

}
